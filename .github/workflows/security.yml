- name: TruffleHog Secret Scanning
  run: |
    docker run --rm -v "$(pwd):/workdir" \
      ghcr.io/trufflesecurity/trufflehog:latest \
      filesystem /workdir \
      --only-verified \
      --no-verification